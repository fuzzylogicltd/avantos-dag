To run locally, install dependencies and then either run the dev script, or run build to compile Typescript. It's assumed that the backend is running on localhost:3000

```
npm i
npm run dev
npm run build
```
